import React from 'react'
import ErrorModal from './ErrorModal'

export default {
  title: 'capstone/SequencePage',
  component: ErrorModal,
}

const Template = () => <ErrorModal />

export const Default = Template.bind({})
