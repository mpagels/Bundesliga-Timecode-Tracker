import React from 'react'
import TimecodeTracker from '../TimecodeTracker'

export default {
  title: 'capstone/TimecodeTracker',
  component: TimecodeTracker,
}

const Template = () => <TimecodeTracker />

export const Default = Template.bind({})
